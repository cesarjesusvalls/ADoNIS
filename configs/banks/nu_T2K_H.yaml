name: nu_T2K_H
probe: CC
beam: spectrum
flux: t2k
material: H          # free proton (materials.py H: free_nucleon -> struck at rest, no cascade)
channels: [res]      # nu_mu CC on free p -> p pi+ is RES-only; no CC-QE on a proton
fsi: false           # free nucleon: pi+ always survives, no nuclear cascade
# 250,000 events/chunk: the nuclear path sizes its FSI record from a calibration run, so the
# chunk is bounded by host memory, not by per-event slots.
n_per_seed: 250000
n_seeds: 2             # 500k by default; a shard scales with --n-seeds/--seed0, never --n-per-seed
seed0: 0
