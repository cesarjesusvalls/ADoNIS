# PRE-FSI (no cascade) primary bank on ARGON-40 -> isolate primary vertex vs cascade for Ar.
# Run: python -u scripts/adonis_generate.py configs/gen_ar_nofsi.yaml
name: t2k_ar_nofsi
material: Ar
n_per_seed: 30000
n_seeds: 20
channels: [qe, res]
fsi: false                 # PRE-FSI: primary interaction products only, skip the cascade
cascade: {}
tracking: {enabled: false}
out_dir: data/oracle
tag: _ar_nofsi
