# ADoNIS Argon-40, HIGH buffers (production preset P=12/g6/nr4/mp6) -- test whether the 0p/1p/2p
# proton-multiplicity migration at the P=6 dev preset is silent recoil/mprot truncation (ktop).
# Writes t2k_cc{0,1}pi_engine_rich_arhi.npz. Run: python -u scripts/adonis_generate.py configs/gen_ar_hi.yaml
name: t2k_arhi
material: Ar
n_per_seed: 30000
n_seeds: 20
channels: [qe, res]
cascade: {P: 12, step: 0.04, max_steps: 260, nn_inelastic: true, mprot: 6}
tracking: {enabled: false, steps: false}
out_dir: data/oracle
tag: _arhi
