# Tagged neutron beam on Ar (FSI-only cascade).
name: beam_neut_Ar
probe: hadron
beam: neut
material: Ar
pmin: 300.0
pmax: 1400.0
n_per_seed: 50000      # events/chunk (dense FSI buffers scale with this)
n_seeds: 10            # chunks -> 500k total by default (jobs override --n-seeds/--seed0 per shard)
seed0: 0
