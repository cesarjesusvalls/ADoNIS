# Buffer-convergence study A: P=6 max_gen=3 n_recoil=2 mprot=4 (the validation buffers).
# Full stats (30k x 56), FIXED cascade engine. Writes t2k_cc{0,1}pi_engine_rich_p6g3.npz.
# Run: python -u scripts/adonis_generate.py configs/gen_p6g3.yaml
name: t2k_p6g3
material: C
n_per_seed: 30000
n_seeds: 56
channels: [qe, res]
cascade: {P: 6, max_gen: 3, n_recoil: 2, step: 0.04, max_steps: 260,
          nn_inelastic: true, mprot: 4}
tracking: {enabled: false, steps: false}
out_dir: data/oracle
tag: _p6g3
